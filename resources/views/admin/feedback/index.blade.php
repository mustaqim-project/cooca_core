@extends('layouts.admin', ['title' => $type === 'bugs' ? 'Kelola Bug' : 'Kelola Request Fitur', 'headerTitle' => $type === 'bugs' ? 'Laporan Bug' : 'Request Fitur', 'headerSubtitle' => 'Pantau dan kelola feedback dari business owner'])
@section('content')<div class="space-y-5"><div class="flex items-center gap-2"><a href="{{ route('admin.feedback.bugs.index') }}" class="px-3 py-2 rounded-lg text-xs {{ $type === 'bugs' ? 'bg-cyan-500 text-slate-950' : 'bg-slate-800 text-slate-300' }}">Laporan Bug</a><a href="{{ route('admin.feedback.features.index') }}" class="px-3 py-2 rounded-lg text-xs {{ $type === 'features' ? 'bg-amber-400 text-slate-950' : 'bg-slate-800 text-slate-300' }}">Request Fitur</a></div><form class="flex gap-2"><select name="status" class="bg-slate-900 border border-slate-700 rounded-lg px-3 py-2 text-xs text-white"><option value="all">Semua Status</option>@foreach($statuses as $value)<option value="{{ $value }}" @selected($status === $value)>{{ str_replace('_', ' ', ucfirst($value)) }}</option>@endforeach</select><button class="px-4 py-2 rounded-lg bg-slate-700 text-xs text-white">Filter</button></form><div class="overflow-x-auto rounded-xl border border-slate-800"><table class="w-full text-xs"><thead class="bg-slate-900 text-slate-400"><tr><th class="text-left p-3">Judul</th><th class="text-left p-3">Bisnis / Pengirim</th><th class="text-left p-3">Status</th><th class="text-left p-3">Progress</th><th class="p-3"></th></tr></thead><tbody>@forelse($items as $item)<tr class="border-t border-slate-800 text-slate-300"><td class="p-3 font-semibold text-white">{{ $item->title }}<div class="text-[10px] text-slate-500 mt-1">{{ ucfirst($item->priority ?? $item->severity) }}</div></td><td class="p-3">{{ $item->business->name }}<div class="text-[10px] text-slate-500">{{ ($item->reporter ?? $item->requester)->name }}</div></td><td class="p-3">{{ str_replace('_', ' ', ucfirst($item->status)) }}</td><td class="p-3 min-w-[130px]"><div class="h-1.5 bg-slate-800 rounded"><div class="h-full bg-cyan-400 rounded" style="width:{{ $item->progress_percent }}%"></div></div><span class="text-[10px]">{{ $item->progress_percent }}%</span></td><td class="p-3 text-right"><a href="{{ route($type === 'bugs' ? 'admin.feedback.bugs.show' : 'admin.feedback.features.show', $item) }}" class="text-cyan-300">Kelola →</a></td></tr>@empty<tr><td colspan="5" class="p-8 text-center text-slate-500">Belum ada data.</td></tr>@endforelse</tbody></table></div>{{ $items->links() }}</div>@endsection
